version: '3'

services:
  app:
    image: hernancr/as222s5_t09_be
    ports:
      - "8085:8085"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DATABASE_USERNAME: ${DATABASE_USERNAME}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD}
      RAPIDAPI_KEY: ${RAPIDAPI_KEY} 
